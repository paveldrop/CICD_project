stages:
  - build
build-code-job:
  stage: build
  script:
    - echo "Start build cat and grep"
    - cd ./src/cat && make
    - cd ../grep && make
  artifacts:
    paths:
      - s21_cat
      - s21_grep
    expire_in: 1 month
